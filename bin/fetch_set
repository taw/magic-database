#!/usr/bin/env ruby

require "set"
require_relative "../lib/magic_database"

ARGV.each do |set_name|
  Gatherer.checklist_for_set(set_name).each do |card_row|
    id = card_row[0]
    GathererDetailsPage.new(id).doc # card_info
    GathererLanguagesPage.new(id).doc
  end
end
