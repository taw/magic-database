#!/usr/bin/env ruby

require "json"

unless ARGV.size == 1
  STDER.puts "Usage: #{$0} AllSets-x.json"
  exit 1
end

data = JSON.parse(open(ARGV[0]).read)

results = []

data.each do |set_code, set_data|
  name = set_data["name"]
  puts [set_code, name].join("\t")
end
